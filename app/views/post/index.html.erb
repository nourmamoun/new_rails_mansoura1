<h1>All Posts</h1>

<% @posts.each do |post| %>
    <div>
        <h2><%= post.title %></h2>
        <p><%= post.body %></p>
    </div>
<% end %>
<%= link_to 'New Post', new_post_path %> |
<%= link_to 'Back', posts_path %>